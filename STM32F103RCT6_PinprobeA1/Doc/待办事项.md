# 待办事项

## P0 — 安全与容错

| # | 内容 | 文件 |
|---|------|------|
| ~~P0.1~~ | ~~上电自检：RCC 时钟、Flash 配置 CRC、RS485 从机响应检查~~ ✓ | main.c |
| ~~P0.2~~ | ~~WatchDog 复位前存 `reset_reason` 到不掉电 SRAM 区，下次启动读取~~ ✓ | iwdg.c, main.c |
| ~~P0.3~~ | ~~ModBus 连续 N 次失败 → 停自动操作 + 告警 LED 闪烁~~ ✓ | state_vector.c |
| ~~P0.4~~ | ~~气缸双线圈写入顺序：先断对向线圈，再合目标线圈~~ ✓ | BsmRelay.c |
| P0.5 | OTA Bootloader 搬运失败恢复：APP 擦写/写入中途失败时不能喂狗死循环卡死；必须更新 flags、清理/保留 BootRequest 策略明确，并允许复位重试或进入可维护状态 | Bootloader/Src/boot_main.c |

## P1 — 可维护性

| # | 内容 | 文件 |
|---|------|------|
| P1.1 | ~~StateVector 栈 128×4→512×4~~ ✓, ~~ModBus 栈缩+优先级 Normal1~~ ✓ | freertos.c |
| P1.2 | ~~SysTimer 替代 osDelay, 精确调度 ModBus+StateVector~~ ✓ | freertos.c/main.c |
| P1.3 | ~~defaultTask 集群健康监控占位~~ ✓, `StateMachine.c` 从编译移除 | .eide/eide.yml |
| P1.4 | `COMMutex` 移除 CmdExec 包裹（只有 ModBusTask 执行） | main.c |
| ~~P1.5~~ | ~~固件版本 + Git hash → `SYSTem:VERSion?`~~ ✓ | scpi-def.c |
| ~~P1.6~~ | ~~RamVector IO 镜像增加整帧快照读取接口，状态机/SCPI 查询改读局部副本~~ ✓ | ram_vector.c/h, state_vector.c, scpi-def.c |
| ~~P1.7~~ | ~~RamVector 命令槽增加原子 Take/Clear 事务；不做队列，执行仍在临界区外~~ ✓ | ram_vector.c/h, cmd_exec.c |

## P2 — 诊断与测试

| # | 内容 | 文件 |
|---|------|------|
| ~~P2.1~~ | ~~运行时间 `SYSTem:UPTime?`（TIM1 累计）~~ ✓ | scpi-def.c, tim.c |
| P2.2 | SCPI 命令错误反馈全覆盖检查 | scpi-def.c |
| P2.3 | IO 板型号识别：读 ModBus 寄存器地址 0，校验匹配 | RS485.c, main.c |
| P2.4 | 出厂测试模式：一键跑全部 IO / 传感器 / 通讯自测 | 新模块 |
| ~~P2.5~~ | ~~汇总检查 `WriteIO()` 返回值，不重试不增加等待，失败记录/上报~~ ✓ | BsmRelay.c, cmd_exec.c |
| ~~P2.6~~ | ~~`ReadIO()` 发送失败直接返回，避免无效等待响应超时~~ ✓ | RS485.c, BsmRelay.c |
| P2.7 | OTA 诊断状态一致性：App 确认运行后同步更新 manifest 持久化状态，或 `SYSTem:OTA:BOOT?` 以 boot flags 为主显示 CONFIRMED，避免仍显示 COMMIT_PENDING | ota_manager.c, scpi-def.c |
| P2.8 | USB 气缸状态闭环：实现完成，待实机验证；StateVector 基于 USB 上/下位传感器、输出方向和短行程超时推导 `cylinder_state[1]`；READY 自动插入 USB，到位后才允许关门；COMPLETE 先发开门，USB 随后跟随回退；SCPI 只读取向量表推导状态 | state_vector.c, ram_vector.c/h, scpi-def.c |
| ~~P2.9~~ | ~~USB `OPEN/CLOSE` 对外语义修订：改为 `CONFigure:CYLInder2 CLOSE`=USB 插入，`CONFigure:CYLInder2 OPEN`=USB 拔出/回退；不改硬件接线，不改 `BsmRelay.c` 中 IO3/IO4 实际输出动作~~ ✓ | scpi-def.c, cmd_exec.c, state_vector.c |
| ~~P2.10~~ | ~~USB 状态返回语义直接修正（不做查询时临时转译）：USB 插入中/到位返回 `CLOSING/CLOSED`，USB 拔出中/到位返回 `OPENING/OPENED`；同步检查 `usb_move_dir`、`usb_insert_fail_logged`、`usb_retract_fail_logged` 的判断条件~~ ✓ | state_vector.c |
| ~~P2.11~~ | ~~USB 自动流程同步修订：READY 双按钮确认后投递 USB 插入语义命令（新版 `VCMD_CYLINDER2_CLOSE`），开门到位/异常兜底投递 USB 回退语义命令（新版 `VCMD_CYLINDER2_OPEN`）；确认动作仍保持当前正确 IO 输出~~ ✓ | state_vector.c, cmd_exec.c |
| ~~P2.12~~ | ~~控制 Demo 同步 USB 语义：按钮“USB 插入”发送 `CONFigure:CYLInder2 CLOSE`，“USB 拔出”发送 `CONFigure:CYLInder2 OPEN`；IO 面板显示保持按实际输出位标注 USB 插入/拔出；USB:AUTO 出厂配置项保留查询提醒~~ ✓ | Tools/pinprobe_gui.py |
| ~~P2.13~~ | ~~客户指令文档/操作说明同步 USB 语义：更新指令表、USB 自动流程、示例、修订记录；保留 `USB:AUTO` 出厂配置项“禁止修改”红色标记；重新生成 HTML/PDF 客户包~~ ✓ | Doc/*.md, Tools/md_to_gts_a4_pages.py |
| P2.14 | USB 语义修订回归验证：手动 `CYLInder2 CLOSE/OPEN`、`READ:CYLInder2:STATe?` 返回值、USB:AUTO READY 插入后关门、COMPLETE 开门后回退、插入/回退超时日志、带/不带 USB 气缸出厂配置读取；静态审查已完成，待固件编译/实机验证 | 固件 + Tools Demo + 实机 |
| ~~P2.15~~ | ~~修复 `USB:AUTO OFF` 设备开门到位后系统状态仍停留 `COMPLETE`：`COMPLETE -> IDLE` 改为以上限位到位且下限位释放为准，不再要求开门输出位保持有效~~ ✓ | state_vector.c |

## P3 — 扩展功能

| # | 内容 | 文件 |
|---|------|------|
| P3.1 | OTA 固件升级（W25Q128 暂存 + SCPI 传输 + Bootloader 写入；主机可通过 CAN 分发同一固件给其他节点） | 新模块，见 `Doc/PinProbeA1_OTA固件升级与CAN多机分发方案.md` |
| P3.2 | 掉电检测（PVD 中断 → 停气缸 → 存状态） | main.c, state_vector.c |
| P3.3 | OTA 分块接收严格校验：非末尾块必须等于 `OTA_SCPI_CHUNK_SIZE`，末尾块必须等于剩余长度；避免短写中间块被标记为已接收 | ota_manager.c |
| P3.4 | OTA 重复块处理：重复 offset 不能无条件返回 OK，应读回比较 payload；不一致时返回错误并要求 abort/begin，或实现可重写块流程 | ota_manager.c |
| P3.5 | OTA Bootloader 擦除策略：刷写新 APP 前擦除整个 APP 分区到 `OTA_CONFIG_BASE_ADDR` 前，避免小固件升级后旧尾部残留 | boot_main.c |

## P4 — 代码质量

| # | 内容 | 文件 |
|---|------|------|
| P4.1 | 散列文件 `useCustomScatterFile: true` | .eide/eide.yml |
| P4.2 | 编译 warning 清零 + MISRA-C 基础检查 | 各文件 |

---

## 多机阶段 (CAN 组网时统一做)

| # | 内容 | 说明 |
|---|------|------|
| M1 | CAN 多机组网（向量表已预留 1024B, CAN 驱动已有） | 节点发现、心跳同步、版本冲突解决 |
| M2 | Flash 故障日志：最近 N 条事件（急停/通讯异常/超时/复位） | W25Q128 大容量更适合 |
| M3 | Flash 配置校验：所有字段合法性检查 | 与 Flash 日志一并规划分区 |
| M4 | Flash 操作计数：气缸/锁动作持久化 | 预防性维护参考 |
| M5 | W25Q128 分区：固件暂存区、快照区、日志区、配置区 | OTA + 快照 + 日志统一管理 |
| M5.1 | 主机 OTA 镜像 CAN 分发：主机先校验本地 W25Q128 镜像，再分发给目标节点 | 子节点本地暂存、校验、READY 后统一 COMMIT |
| M6 | 向量表 CAN 同步：version 驱动, CRC32 校验, 冲突诊断 | 主线已实现，单机启用 |
| M7 | 设备总寿命累计 (跨复位持久化至 Flash/备份寄存器) | 与 Flash 日志一并规划 |

---

## 已完成 ✓

- TIM1 毫秒计时、WatchDog 独立线程
- LOCK 防重复触发、气压检测修正、激光急停保护
- RS485 TX 超时、CRC16 查表、帧长校验、WaitRx
- rs485_io_ok 隔离、门限位消抖
- WriteIO 返回 bool、SCPI 读写全部走向量表
- RamVector 1024B 对齐主线、node 0=单机 1-8=多机
- ModBusTask RS485 独占、StateVectorTask 纯逻辑
- RS485 模块 → Hardware/ModBus
- UART1/3 DMA 双缓冲审查、U1_Printf 非阻塞、命名规范
- SysTimer 精确节拍替代 osDelay
- 风险模式、防夹手时序、E-STOP 常开/常闭可配
- IDN 字段可配（Flash 持久化）
- VECTOR_DEBUG 动作/状态/事件日志
- 流程文档 + 待办文档
- P0.1 上电自检 (RCC/Flash/RS485, 500ms等PC串口)
- P0.2 复位原因捕获 (RCC->CSR, POR/IWDG/NRST/Soft)
- P0.3 RS485 故障保护 (连续10次→黄灯+停自动操作)
- P0.4 气缸写入先断对向线圈，再合目标线圈
- P1.5 SYSTem:VERSion? (固件版本+Git hash)
- P1.6 RamVector IO 整帧快照读取，状态机/SCPI 查询改读局部副本
- P1.7 RamVector 命令槽原子 Take/Clear，执行保持在临界区外
- P2.1 SYSTem:UPTime? (运行秒数)
- P2.5 WriteIO 返回值汇总检查，失败写入 AppLog
- P2.6 ReadIO 发送失败直接返回
